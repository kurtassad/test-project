# README #

This assumes you have npm installed.

To download dependencies run:

`npm install`

You can run the app with To run the app, you can run:

`npm dev`

And navigate to http://localhost:3000/

To run the playwright test, while serving the app, you can run:

`npm test`